<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/fill" /> <!-- Background color -->
    <corners android:radius="15dp" /> <!-- Corner radius -->
    <stroke android:color="@color/stroke" android:width="2dp" />  <!-- Optional border -->
</shape>
